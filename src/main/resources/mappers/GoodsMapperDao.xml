<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iyy.mapper.GoodsMapperDao">
  <resultMap id="BaseResultMap" type="com.iyy.entity.Goods">
    <id column="goods_id" jdbcType="INTEGER" property="goodsId" />
    <result column="goods_name" jdbcType="VARCHAR" property="goodsName" />
    <result column="goods_sku" jdbcType="VARCHAR" property="goodsSku" />
    <result column="goods_type_id" jdbcType="INTEGER" property="goodsTypeId" />
    <result column="goods_brand_id" jdbcType="INTEGER" property="goodsBrandId" />
    <result column="goods_spec" jdbcType="VARCHAR" property="goodsSpec" />
    <result column="goods_weight" jdbcType="DOUBLE" property="goodsWeight" />
    <result column="goods_quantity" jdbcType="INTEGER" property="goodsQuantity" />
    <result column="goods_unit" jdbcType="VARCHAR" property="goodsUnit" />
    <result column="piece_weight" jdbcType="DOUBLE" property="pieceWeight" />
    <result column="goods_manufacture" jdbcType="VARCHAR" property="goodsManufacture" />
    <result column="sales_method" jdbcType="VARCHAR" property="salesMethod" />
    <result column="create_user" jdbcType="VARCHAR" property="createUser" />
    <result column="create_date" jdbcType="TIMESTAMP" property="createDate" />
  </resultMap>
  <resultMap extends="BaseResultMap" id="ResultMapWithBLOBs" type="com.iyy.entity.Goods">
    <result column="goods_img" jdbcType="LONGVARBINARY" property="goodsImg" />
  </resultMap>
  <sql id="Base_Column_List">
    goods_id, goods_name, goods_sku, goods_type_id, goods_brand_id, goods_spec, goods_weight, 
    goods_quantity, goods_unit, piece_weight, goods_manufacture, sales_method, create_user, 
    create_date
  </sql>
  <sql id="Blob_Column_List">
    goods_img
  </sql>

  <select id="queryGoodsList" resultMap="BaseResultMap">
    select
        *
    from
        tb_goods
  </select>

  <select id="queryGoodsInfoList" resultType="com.iyy.vo.GoodsListInfo" parameterType="com.iyy.service.params.QueryGoodsListParams">
    select
      g.goods_id as goodsId,
      g.goods_name as goodsName,
      g.goods_sku as goodsSku,
      g.goods_type_id as goodsTypeId,
      g.goods_brand_id as goodsBrandId,
      g.goods_spec as goodsSpec,
      g.goods_weight as goodsWeight,
      g.goods_quantity as goods_quantity,
      g.goods_unit as goodsUnit,
      g.piece_weight as pieceWeight,
      g.goods_manufacture as goodsManufacture,
      g.sales_method as salesMethod,
      g.create_user as createUser,
      g.create_date as createDate,
      gt.goods_type_name as goodsTypeName,
      b.brand_name as brandName,
      CASE WHEN g.sales_method = '10' THEN '称重' ELSE '按件' END AS salesMethodStr
    from tb_goods g
    inner join tb_goods_type gt
    on g.goods_type_id = gt.goods_type_id
    inner join tb_brand b on g.goods_brand_id = b.brand_id
    <where>
      <if test='goodsSku != null and goodsSku != "" '>
        and (g.goods_name like concat(#{goodsSku}, '%') or g.goods_sku = #{goodsSku})
      </if>
      <if test="brandId != null">
        and g.brand_id = #{brandId}
      </if>
      <if test="goodsTypeId != null">
        and g.goods_type_id = #{goodsTypeId}
      </if>
    </where>
  </select>

  <select id="queryGoodsBySku" resultMap="BaseResultMap">
    select * from tb_goods where goods_sku = #{goodsSku}
  </select>

</mapper>